'use strict';

/**
 * @ngdoc service
 * @name  $rootElement
 *
 * @description
 * The root element of Angular application. This is either the element where {@link
 * directive:ngApp ngApp} was declared or the element passed into
 * {@link function:angular.bootstrap}. The element represent the root element of application. It is also the
 * location where the applications {@link module:auto.$injector $injector} service gets
 * published, it can be retrieved using `$rootElement.injector()`.
 */


// the implementation is in angular.bootstrap
